import { encrypt, generateKey } from './encrypt'
import { decrypt } from './decrypt'
import { hashData, compareData } from './hash'

export default { encrypt, decrypt, generateKey, hashData, compareData }
